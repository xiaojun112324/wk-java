server:
  port: 8888
  address: 127.0.0.1

spring:
  application:
    name: f2pool-backend
  jackson:
    time-zone: Asia/Shanghai
    date-format: yyyy-MM-dd HH:mm:ss
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://121.127.231.209:3306/wk-mysql?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
    username: wk-mysql
    password: 8P44njJ3WckjxiW7
  data:
    redis:
      host: 121.127.231.209
      port: 6379
      password: EWdeXZ2SkT73bdQQnW64
      database: 5
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.nologging.NoLoggingImpl
    map-underscore-to-camel-case: true

springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html

jwt:
  secret: ${JWT_SECRET:change-this-to-a-very-long-random-secret-key-please-1234567890}
  expire-seconds: ${JWT_EXPIRE_SECONDS:604800}

app:
  image-domain: https://api.kuaiyi.info

logging:
  level:
    org.mybatis: WARN
    com.baomidou: WARN
    org.apache.coyote.http11.Http11Processor: ERROR
